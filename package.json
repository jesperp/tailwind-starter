{
  "name": "tailwind-starter",
  "version": "0.0.1",
  "description": "",
  "scripts": {
    "build:prod": "NODE_ENV=production postcss --verbose src/**/*.css --dir public/css",
    "build:dev": "postcss --verbose src/**/*.css --dir public/css",
    "watch": "postcss --verbose src/**/*.css --dir public/css --watch",
    "serve": "live-server public/",
    "start": "run-p serve watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "autoprefixer": "^10.0.4",
    "cssnano": "^4.1.10",
    "postcss": "^8.1.14",
    "postcss-cli": "^8.3.0",
    "tailwindcss": "^2.0.2"
  },
  "devDependencies": {
    "live-server": "^1.2.1",
    "npm-run-all": "^4.1.5"
  }
}
